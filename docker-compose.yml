services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: tracking_lead_system
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: nada2006
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:

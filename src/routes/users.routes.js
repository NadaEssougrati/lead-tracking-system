const router = require("express").Router(); const prisma=require("../lib/prisma"); const {safeUser,hashPassword}=require("../lib/helpers"); const {authenticate,allowRoles}=require("../middleware/auth");
router.use(authenticate);
router.get("/", async(req,res,next)=>{try{const users=await prisma.utilisateur.findMany({where:req.query.actif===undefined?{}:{actif:req.query.actif==='true'},orderBy:{dateCreation:'desc'}});res.json({success:true,data:users.map(safeUser)});}catch(e){next(e)}});
router.post("/",allowRoles("Administrateur"),async(req,res,next)=>{try{const {motDePasse,...data}=req.body;if(!data.nom||!data.prenom||!data.email||!motDePasse)return res.status(400).json({success:false,message:'Champs requis manquants.'});const user=await prisma.utilisateur.create({data:{...data,email:data.email.toLowerCase(),motDePasse:await hashPassword(motDePasse)}});res.status(201).json({success:true,data:safeUser(user)});}catch(e){next(e)}});
router.patch("/:id",allowRoles("Administrateur"),async(req,res,next)=>{try{const {motDePasse,...data}=req.body;if(motDePasse)data.motDePasse=await hashPassword(motDePasse);const user=await prisma.utilisateur.update({where:{id:req.params.id},data});res.json({success:true,data:safeUser(user)});}catch(e){next(e)}});
router.patch("/:id/password",authenticate,async(req,res,next)=>{try{if(req.user.id!==req.params.id && req.user.role!=='Administrateur')return res.status(403).json({success:false,message:'Non autorisé.'});const {currentPassword,newPassword}=req.body; if(!newPassword||newPassword.length<6)return res.status(400).json({success:false,message:'Le nouveau mot de passe doit contenir au moins 6 caractères.'});const user=await prisma.utilisateur.findUnique({where:{id:req.params.id}});if(!user)return res.status(404).json({success:false,message:'Utilisateur introuvable.'}); if(req.user.role!=='Administrateur'){const valid=await bcrypt.compare(currentPassword||'',user.motDePasse); if(!valid)return res.status(401).json({success:false,message:'Mot de passe actuel incorrect.'});} const updated=await prisma.utilisateur.update({where:{id:req.params.id},data:{motDePasse:await hashPassword(newPassword)}});res.json({success:true,data:safeUser(updated)});}catch(e){next(e)}});
module.exports=router;
